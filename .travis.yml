language: julia

env:
  - IMAGE=texlive-julia-minimal

os: linux

dist: trusty

sudo: required

services:
  - docker

notifications:
  email: false

script:
  - DOCKER_USER=${DOCKER_USER:-tkapp}
  - IMAGE=${IMAGE:-texlive-julia-minimal-docker}
  - ./build-script.sh
  - ./test-script.sh
